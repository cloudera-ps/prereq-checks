- ntpd check
- Isilon check
  - hdfs -ls + -cat
- Check DNS resolution (dig, nslookup, python code)
- Check for unneeded services
  - bluetooth
  - cups
  - iptables
  - ip6tables
  - postfix
- Check that IPv6 is disabled
  
- Check multiple hosts
- Fix issues
- check MySQL auto-starts on boot
- check MySQL config
- check fs (type and mount options) + reserved space

check that /tmp does not have noexec turned on
check that data drives are mounted with noatime
check that data drives are ext3 or ext4 (maybe even xfs)
